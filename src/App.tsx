import './App.css';
import { Button } from 'react-bootstrap';

const initialState = {
  fromLanguage: 'auto',
  toLanguage: 'en',
  fromText: '',
  result: '',
  loading: false,
};

function reducer(state, action) {

}

function App() {
  return (
    <>
      <Button>Hola</Button>
    </>
  );
}

export default App;
